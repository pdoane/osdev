This is a hobby OS project for learning more about low-level hardware.

Software used:
- MinGW
- GCC
- NASM
- Make

Emulator steps:

  $ make
  $ qemu.bat

Hardware steps:

  Format a FAT16 drive, update the boot sector and copy the loader binary

  $ make
  $ tools/set_boot.exe //./<drive letter>: boot/boot.bin
  $ cp boot/loader.bin <drive letter>:

  Boot!
